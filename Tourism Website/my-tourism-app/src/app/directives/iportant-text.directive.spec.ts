import { IportantTextDirective } from './iportant-text.directive';

describe('IportantTextDirective', () => {
  it('should create an instance', () => {
    const directive = new IportantTextDirective();
    expect(directive).toBeTruthy();
  });
});
